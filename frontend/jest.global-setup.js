// Global setup for Jest tests\n// Runs once before all test suites\n\nmodule.exports = async () => {\n  // Set up test database or external services if needed\n  console.log('🧪 Setting up test environment...');\n  \n  // Mock environment variables\n  process.env.NODE_ENV = 'test';\n  process.env.NEXT_PUBLIC_KHIVE_API_URL = 'http://localhost:8000';\n  process.env.NEXT_PUBLIC_KHIVE_WS_URL = 'ws://localhost:8767';\n  \n  // Any global test setup can go here\n  // For example: starting test servers, initializing test data, etc.\n};