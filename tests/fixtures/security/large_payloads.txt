# Large payload file for DoS testing

# Small payload (1KB)
SMALL_PAYLOAD="A" * 1024

# Medium payload (10KB)
MEDIUM_PAYLOAD="B" * 10240

# Large payload (100KB)
LARGE_PAYLOAD="C" * 102400

# Very large payload (1MB) - Use with caution
# VERY_LARGE_PAYLOAD="D" * 1048576

# Payload with repeating dangerous patterns
DANGEROUS_REPEATED_PAYLOAD=("'; DROP TABLE users; --" + "X" * 100) * 100

# Deep nesting payload for JSON deserialization attacks
DEEP_NESTING_JSON='{"level0": {"level1": {"level2": {"level3": {"level4": {"level5": {"level6": {"level7": {"level8": {"level9": {"level10": {"dangerous": "../../../etc/passwd"}}}}}}}}}}}' * 50

# Billion laughs equivalent for text
BILLION_LAUGHS_TEXT="lol" + ("A" * 1000 + "lol") * 1000
